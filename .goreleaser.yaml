version: 2

project_name: scafctl-plugin-cosign

builds:
  - id: plugin
    main: ./cmd/scafctl-plugin-cosign/
    binary: scafctl-plugin-cosign
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/oakwood-commons/scafctl-plugin-cosign/internal/cosign.version={{ .Version }}

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt

changelog:
  use: github-native
